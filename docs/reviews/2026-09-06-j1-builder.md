# TCP-02 J1 experimental ledger builder evidence — 2026-09-06

Status: authored and focused tests passing in the isolated J1 candidate. TCP-02 is incomplete.
Parent canonical gate and fresh independent read-only review are PENDING. This is builder
evidence, not independent review, accepted upstream authority, or runtime qualification.

Scope authority: reviewed `/private/tmp/corvint-tasks-build-20260906/tcp02-journal-plan-r3.md`
and parent `/private/tmp/corvint-tasks-build-20260906/tcp02-j1-steering.md`, points 1..5, read
before completion. No delegation, Git action, network, real queue, Corvint source edit, runtime
spawn, durable producer, or full make verify was performed. Fixtures use bounded temporary
files; no server, child runtime, database, or process fixture was needed. Historical reports,
safeopen, authority and archive production/staging implementations were not edited.

## Modified files

- New `internal/journal/{audit.go,source.go,records.go,index.go,redo.go,audit_test.go}`.
- `internal/snapshot/{head.go,snapshot_test.go}`; new `receipt.go`, `receipt_test.go`.
- `internal/mutation/{replay.go,outcome.go,mutation_test.go}`; new `replay_journal_test.go`.
- `internal/wire/{codes.go,wire_test.go}`: narrowly added closed UNSUPPORTED refusal and count.
- `internal/fixture/fixture.go`, `internal/archive/archive_test.go`: actual minimal genesis,
  request proofs and corresponding exact fixture counts. Malformed negatives remain tests.
- `docs/SPEC.md`, `docs/ROADMAP.md`, `docs/agent-memory/{fixes.md,tests.md}`, this new report.

## Delivered behavior and clarified mechanics

Shared strict snapshot decoders validate closed receipt paths, paired sorted duplicate-free
pre/post sets, complete canonical afterimages and consumed-byte identities, closed enums,
64-byte request IDs, exact inline limits, and the single all-null UNPAUSE barrier deletion.
The pure deletion classifier covers absent, exact-pre and third-digest states without unlink.

Genesis is noncircular: head.initSha256 hashes complete receipt1 bytes. Its exact minimal
post set is VERSION with actual retained exact-byte blob, queue, policy, empty reservations,
one pinned INIT descriptor, and a request entry iff requested; every pre is null. Initial
tickets/import-map and other extras refuse. Identity/version mismatch and missing VERSION
post/blob fail before any REDO_PENDING result. Headless linked genesis is pending; precommit
remnants without a linked receipt remain uninitialized with staging presence and no cleanup.

Parent clarifications were incorporated without substituting an unstated interpretation:

1. Recorded refusals may name receiptSeq while retaining null resulting revisions;
   uncommitted Apply/Adopt refusals remain receipt-free.
2. Minimal INIT has the exact post set above; it is not an adoption protocol.
3. Receipt pre is a physical redo condition, not a canonical historical predecessor.
   The RECONCILE witness records edited D as pre and C-prime.previousRecordSha256 = canonical C.
   Global pre == preceding post equality is not imposed. Historical reconciliation authority,
   discarded-byte retention and ticket predecessor-chain semantics remain NOT_OBSERVED.
4. COMPLETED allows both revisions or neither. Indexed ticket successes must equal the
   matching target-ticket afterimage's actual revision pair; no target, absent/mixed revisions,
   or fabricated 99/99 against 2/1 refuse. No-ticket INIT/admin successes carry null revisions.
5. Public Audit remains strict. Request lookup skips ONLY intent projection equality and
   untracked-intent checks in both projection loops. It still validates the entire chain,
   required blobs, every request/private projection, and the stable head/inventory/intent
   tuple. Intent projection agreement is explicitly NOT_OBSERVED; no error is caught and
   ignored. Stable D != C permits replay/verified request absence, while missing/corrupt ANY
   required request projection refuses both. Lookup never supplies an overwrite/actor grant.

The native source uses existing no-follow read opening, bounded listing chunks and per-file
bounds. Every receipt filename is enumerated, including slots beyond head+2. Chain bytes are
streamed, retaining bounded digest/path metadata and only selected afterimages. State scanning
keeps the existing 2,104,096 budget; the separate intent root/ticket budgets do not consume it.
Selected raw output uses the existing 256 MiB aggregate intent-state bound; oversized selection
fails instead of truncating. Streamed request lookup has no arbitrary small history cap.
Every lookup completes a fresh audit and returns `(entry,found,error)`; post-success I/O errors
refuse Apply/Adopt rather than producing fresh plans. Observation identity remains available
for serial callers; it is not a cross-snapshot atomic transaction or trusted caller binding.

## Focused validation

All Go tests used `GOTOOLCHAIN=local`,
`GOCACHE=/private/tmp/corvint-tasks-build-20260906/go-cache`, `GOMAXPROCS=2`, `GOFLAGS=-p=2`.
Commands ran serially without PTYs; no canonical gate overlapped them.

- `go test -count=1 ./internal/journal ./internal/mutation ./internal/snapshot ./internal/wire`
  — PASS after steering points 1..5. Journal rerun after the independent state/intent scan
  budget correction also passes.
- `go test -count=1 ./internal/archive ./internal/cli` — PASS after final outcome/fixture
  integration, including >10,000-member archive and prior staging/read regressions.
- Focused `go vet ./internal/journal ./internal/snapshot ./internal/mutation ./internal/archive
  ./internal/wire` — PASS (rerun on final source).
- `gofmt -l` on every changed Go file — no output.

Adversarial witnesses cover genesis completeness/identity, seq/prev/generation, +3/gaps/pending,
closed destinations/pairs/deletion, request filename/history/outcome/revisions, exact replay
and conflict, post-construction I/O failure, strict divergence versus request lookup, required
historical blob availability, size/inline limits, symlinks, bounded enumeration and movement
including body errors. Byte/mode/mtime witnesses exercise normal, pending, headless, staging,
forked and divergent observations. Named TM/AS tests are indexed in SPEC §3.4.

A modest 97-receipt fixture with >4 MiB historical afterimages samples reachable heap after
collection during streaming and passes a bounded-growth witness. It proves neither worst-case
production RSS nor low allocation churn; no million-receipt or multi-GiB fixture was allocated.
Initial focused runs caught paired-path fixture assumptions, archive member/scan count changes,
and the closed-code count; each was repaired locally without relaxing malformed negatives.

## Explicit boundaries and holds

J1 reports structural consistency and projection agreement only. Known record codecs are not
historical acceptance validators; later profiles without full decoders yield semantic UNKNOWN.
Historical authorization/acceptance, actor authentication, liveness and runtime qualification
remain NOT_OBSERVED. Archive verification remains its existing chain/digest validation:
its retained semantic input omits required blobs, so full historical semantic integration is
NOT_OBSERVED, never inferred from manifest digests or missing bytes.

No J1 ambiguity remains unresolved after steering points 1..5. J2 capacity arithmetic/escrow
and J3 durable writers, stable INIT retry and staged-artifact charging remain HELD. Future
reconciliation needs a validated canonical-record retrieval entrypoint during divergence;
strict Audit(selected) refuses and its arbitrary partial results are not authority. That
wiring, complete ticket-history semantics, archive semantic byte sourcing, reservations,
runtime, restore/prune, CLI mutations and real-queue cutover are outside this increment.

Parent owns canonical `make verify`, fresh independent review and any later promotion.
GP is NOT_RUN. No syscall durability, performance, runtime or full TCP-02 completion claim.
