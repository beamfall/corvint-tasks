# Parent schema clarifications — J1, 2026-09-06

These are explicit clarifications of unpublished experimental mechanics within the reviewed J1 scope, not accepted upstream changes or writer authorization. Record them in SPEC and the builder report.

1. **Recorded refused outcomes may carry receiptSeq.** The reviewed request-post rule binds every indexed complete outcome to its receipt, including a recorded refusal. Amend the outcome codec narrowly to permit non-null receiptSeq on a refused outcome while retaining null resulting revisions and other existing outcome-dependent restrictions. Pure uncommitted Apply/Adopt refusals must remain receiptSeq:null. A decoded sequence is an assertion, never proof of commitment by itself; the ledger binder checks equality and actual receipt/post evidence. Preserve negatives for forbidden resulting revisions and mismatched outcome/index/receipt sequences, add recorded-refusal positive binding and uncommitted-refusal null witnesses. internal/mutation/outcome.go and its relevant tests are within the necessary API integration claim.

2. **Minimal closed INIT for J1.** Limit receipt1 post set to exactly VERSION, intent/queue.json, intent/policy.json, empty reservations.json, one pinned taskman-init/0 descriptor, plus the request-index post iff requestId is non-null. All paired pre digests are null. Do not permit initial ticket/import-map posts in this increment. Extra genesis posts refuse explicitly; later receipts can establish canonical ticket/import-map facts within their structural profiles. This is the smallest experimental genesis needed by J1, not a real existing-queue adoption protocol. Keep archive historical-semantic integration NOT_OBSERVED where old archive fixtures/data lack these proofs; do not silently bless extra unjournalled projections or claim a complete migrated queue.

No further human clarification is needed for these two mechanical points. Continue focused implementation; parent owns full gate and independent review. Main-branch promotion is separately pending approval and does not block this isolated branch.

3. **Physical redo preconditions versus canonical history.** Independent archive/capacity expert confirmed: receipt.pre.sha256 is the recorded physical redo precondition, not the canonical revision predecessor. Validate its Digest|null type, closed path, pairing and INIT/deletion constraints. Do not impose global historical pre == preceding canonical post equality. A differing pre on RECONCILE is structurally admissible but does not establish authorization or retention of discarded-byte evidence; historical reconciliation validation remains NOT_OBSERVED. Ticket previousRecordSha256, if checked, refers to preceding canonical record rather than receipt.pre; if not covered leave that explicitly NOT_OBSERVED. Witness canonical C hash c, edited projection D hash d!=c, RECONCILE pre=d and adopted C-prime.previousRecordSha256=c: accept structural chain/current post projection, never call d the canonical predecessor or claim historical authorization.

A separate no-ticket COMPLETED outcome / null revisions integration question is currently with an independent Gate A reviewer. Do not invent a ticket revision to make indexed INIT/admin outcomes parse. Continue unrelated J1 work; parent will append its disposition.

4. **No-ticket successful outcomes — Gate A resolved.** Independent native_read_audit confirmed the generic outcome correction is mechanically necessary and minimal; its one MED binder correction is included here. DecodeOutcome COMPLETED permits either both resulting revisions present or both null, rejecting mixed nullness. For a receipt with a non-null ticketId and COMPLETED indexed outcome, require both values to equal the decoded matching target-ticket afterimage's revision and acceptanceRevision, not merely be present. Missing target afterimage or 99/99 claimed against posted 2/1 refuses. For no-ticket administrative COMPLETED, both resulting revisions must be null. Recorded refusals may carry the matching receiptSeq and must have null resulting revisions. Pure Apply/Adopt ticket results still carry correct revisions; pure uncommitted refusals keep receiptSeq:null. Standalone decoded fields are assertions until the receipt binder verifies agreement.

Required positive/negative evidence: indexed minimal INIT success with ticketId:null, receiptSeq:1 and both revisions:null; ticket success with exact posted revision pair; mixed nullness; ticket success without revisions; admin success with revisions; mismatch against target afterimage; refused outcomes with revisions. These prove structural agreement only; canonical historical authorization/acceptance stays NOT_OBSERVED. No new profile tag or fabricated ticket revisions. Amend the explicit unpublished experimental SPEC mechanics; no runtime/real queue promotion. Gate A review is recorded in tcp02-j1-outcome-gate-a.md; no HIGH concerns, sole MED is resolved by exact post-value binding above.

5. **Request lookup must remain possible during stable intent divergence — Gate A PASS.** Independent native_read_audit confirmed an integration defect: StreamedIndex calling strict all-projection Audit rejects D!=canonical C before ADOPT_FILE can even check request history. Keep public Audit strict. In the request adapter's complete audit path, skip ONLY intent/* projection equality/untracked-intent checks in BOTH projection loops; retain every existing non-intent check, full receipt/blob/request binding and before/after head/inventory/intent tuple stability. Report intent projection agreement NOT_OBSERVED. Never catch-and-ignore INTENT_DIVERGED, since early return can skip corrupt request projections. No branch, overwrite, actor or reconciliation grant is minted.

Witness: valid ledger with C and recorded request R; replace current ticket projection with stable bounded D!=C. Strict Audit refuses INTENT_DIVERGED; Lookup(R) returns bound original outcome, Lookup(newID) gives false,nil. Deleting/corrupting ANY required request projection makes both lookups refuse. intent.Divergence(D,C,nil) remains INTENT_DIVERGED and lookup is byte/mode/mtime pure.

Explicit adjacent hold: strict Audit(selected canonical ticket) still refuses on D; future reconciliation needs a validated canonical-record retrieval entrypoint. Do not treat arbitrary partial audit results as authority or claim complete reconciliation wiring from this adapter repair. That wiring remains outside J1. Independent review is recorded in tcp02-j1-index-divergence-gate-a.md; no HIGH/MED concern in this narrow correction.
