# Settled fixture pause/unpause delivery — practical Gate A

Continue the owner's request through the next specified local prerequisite while separate Corvint-task authority is pending. Astra/high for journal/barrier invariants; one builder and same read-only reviewer. No nested work. Keep prior verified stages intact. Earliest proof: init/create -> pause -> status ADMISSION -> ticket mutation remains usable -> unpause -> status barrier absent; retries never reapply an old pause/unpause.

Release only settled fixture PAUSE/UNPAUSE under next decision0008, using existing transaction.Model exact request digests and templates (§5.5). Role OWNER/OPERATOR remains an unauthenticated local binding. No runtime/reservation release, attempts, admission, restore, new policy, real queue, or qualification. Pending receipt and active descriptor recovery stay explicitly unsupported by the new entrypoint. Do not widen the existing mutation redo path in this slice.

Claim: internal/authority/session.go (narrow wrapper around existing reviewed removeBarrier only); internal/store/{barrier.go(new),barrier_test.go(new),store.go,guards.go,export_test.go}; internal/journal/{audit.go,records.go,audit_test.go}; internal/cli/{barrier.go(new),barrier_test.go(new),cli.go}; README.md, docs/SPEC.md, ROADMAP.md, decisions/0008-local-fixture-barriers.md(new), reviews/2026-09-19-writer-repair.md. Shared wiring serial; source frozen during gate. Supervisor/cleanup witness already qualified for running gates (not product runtime).

1. Add Reader.BarrierRemoval(selectedPaths...) with a private value-copy mode. Complete settled audit permits divergence only for present regular intent/tickets/* canonical projections. All canonical ticket paths must physically exist; missing or extra tickets refuse. Queue/policy/private state stay strict; all history and canonical record codecs stay validated. Pending refuses; active descriptor result remains explicit. Success projection axis TICKETS_NOT_COMPARED, never AGREES. Ordinary Audit/RequestIndex/Reconciliation unchanged. This implements SPEC's existing UNPAUSE exception without trusting partial results or reading each ticket in separate audits.
2. Store.Barrier(ctx,repo,actor,BarrierRequest{QueueID,RequestID,Operation},now), operation PAUSE/UNPAUSE only. Validate request/actor before any lock. Existing state/VERSION/marker/primary guards apply; ALL exempt only UNPAUSE in addition to existing reconciliation exemptions. Full validated request lookup first: exact replay/conflict before fresh barriers/intent (except mandatory entry guards), no effects. Pending refuses, no redo. Fresh inventory + full selected audit (strict PAUSE, BarrierRemoval UNPAUSE) supplies canonical queue/policy/all tickets to existing Model. Bind complete audit Identity with second same-mode audit, refuse active staging, recheck guards before apply. Branch is not required for PAUSE/UNPAUSE by existing Model/SPEC. No new mutable request fields.
3. Applier deletion: expose Session.RemoveBarrier(expectedDigest) delegating existing pinned/no-follow/exact-pre unlink+directory-sync implementation. Decode plan's receipt to find explicit null barrier post; only UNPAUSE/barrier with nonnull pre accepted (existing codec enforces). Apply deletion after receipt and all ordinary posts, before head, matching reviewed fixture harness. No artifact/slot for deletion, no generic delete API, no history deletion or capacity change. Other plans untouched. Native returned-precommit-fault test through existing per-call private hook proves barrier/head/request/receipt unchanged; normal success proves null-post receipt and absence. Pending UNPAUSE remains conservatively refused by new commands until recovery delivery, explicitly documented.
4. CLI `pause|unpause --request-id ID [--role OWNER|OPERATOR]`; closed flags, duplicate/unknown/missing/inapplicable refuse. Resolve/read head only for request queue identity; Store revalidates under lock. No intent.Load so UNPAUSE accepts malformed/empty but present ticket D. Standard mutateResult envelope, ticketId empty for admin. Help inventory and exact SPEC syntax updated.
5. Tests: pause/unpause/nochange/replay; retry pause after later unpause doesn't pause again; same request cross-operation/actor conflict; ADMISSION ordinary mutation; ALL unpause exemption and pause refusal; unpause across multiple malformed/empty tickets preserves D/orphans, pause fails divergence; missing/extra ticket or queue/policy/private corruption refuses; pending/staging/marker/identity/unsupported-version/invalid role no effects; precommit returned fault before barrier deletion; CLI full workflow/status/flags. Named TM/AS IDs. Bounded executable smoke with checked-in fixture inputs.
6. Focused path, fresh independent GateB and one frozen make verify; at most remaining one review-repair cycle. Preserve all evidence and owned process cleanup; no performance conclusion from concurrent unrelated gate.

Gate A PASS. Added per-call pre-deletion test hook (production nil, test-only exported private barrier alias) alongside the existing precommit hook. Tests cover returned fault and actual exact-pre mismatch after receipt, before head. Current null-post redo remains unsupported by every callable recovery path, explicitly documented.
