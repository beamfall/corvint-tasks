# Gate A — settled fixture PAUSE/UNPAUSE

**PASS: no HIGH concern.** Reviewed `barrier-plan.md` against current source and the relevant SPEC §5.2/§5.5 mechanics. Read-only review; no tests, source edits, nested agents or background processes. Astra/high selected for invariant reasoning; live settings are not independently inspectable. Fresh usage telemetry is 8% weekly consumed; no reset used.

The scope fits the existing local fixture model. SPEC §5.5 and `internal/transaction/model.go:337` preserve PAUSE ADMISSION/OPERATOR NoChange, refusal of other barriers, and UNPAUSE absent-barrier NoChange. Exact replay precedes fresh transition checks; mandatory startup guards remain earlier. The model requires branch equality only for intent-changing operations (`model.go:349`), so omitting a branch requirement for these two administrative operations is consistent. Real-queue/runtime/authentication authority remains excluded.

## Implementation risks to retain

- **MED, addressed by the stated plan:** the UNPAUSE exception is narrower than RequestIndex's intent exemption. In `internal/journal/records.go:418`, confirm every canonical ticket has a present regular physical file before bypassing ticket digest equality. Preserve the second loop's extra-ticket refusal and ordinary queue/policy/private equality. Canonical ticket codecs, scope and complete inventory remain validated. Missing files must not enter through a blanket prefix `continue`. This matches `internal/transaction/model.go:549` and SPEC's explicit present-projection boundary.
- **MED, ordering check:** decode and validate the fixed `barrier.json` deletion/pre digest before effects, then call the narrow Session wrapper after receipt and ordinary posts, before head. `internal/authority/fixture_session.go:814` already supplies pinned target, digest, identity, unlink and directory-sync checks. Any deletion/sync failure must return before head publication. The existing precommit returned-fault test plus normal success checks are useful; a bounded deletion-failure witness would directly protect the newly added receipt→unlink→head boundary without implying crash qualification.
- **LOW, documentation precision:** a postcommit UNPAUSE failure remains pending and unsupported by all current callable recovery paths. `internal/store/redo.go:121` still rejects a null post. State that limitation explicitly, rather than implying only the new pause/unpause entrypoint refuses it. Preserve that conservative refusal in this slice.

The practical sequence is sound: prove normal pause→status→ordinary mutation→unpause first, then multiple divergent/empty tickets, retained orphans, replay and returned-fault boundaries. Use the existing opaque model and removal primitive; no new generic deletion mechanism, limits, runtime or recovery machinery is needed. One bounded final gate after focused tests, frozen source/HEAD, existing supervisor cleanup and the existing reviewer suffice. No additional owner choice or governing-source recovery is required for these already specified fixture mechanics.
