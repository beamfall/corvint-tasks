# Bounded local reconciliation delivery (Gate A)

Owner's "get it there" authorizes implementation/testing of already specified local mechanics. Missing frozen originals block upstream ambiguity/qualification, not this local fixture slice. Record next free decision 0007 as this narrow delivery of SPEC §3.1/§3.3/§5.5; do not change those mechanics or authorize real queues, runtime, foreign writes, history deletion, or shared-agent authentication.

Scope: one settled fixture ticket, KEEP_JOURNAL and ADOPT_FILE, using existing pure transaction.Model operations. No runtime or pending/staging recovery. Existing ticket mutation behavior must remain verified. Model choice Astra/high for snapshot/replay boundaries; one builder/reviewer, no nesting, max two repair cycles. Earliest proof: create → manual edit → mutation refuses → explicit reconciliation → ordinary mutation works.

Claimed files: internal/journal/{audit.go,records.go,audit_test.go}; internal/store/{guards.go,mutate.go,reconcile.go(new),reconcile_test.go(new)}; internal/cli/{cli.go,reconcile.go(new),reconcile_test.go(new)}; docs/SPEC.md, ROADMAP.md, decisions/0007-local-fixture-reconciliation.md(new), agent-memory/fixes.md, reviews/2026-09-19-writer-repair.md. If extracting existing replay or canonical-input helpers, keep within these files and preserve all Stage1 regressions.

1. Add journal.Reader.Reconciliation(targetID string, selectedPaths ...string), a complete audit that permits only the named target projection to diverge. Parse target ID/scope; derive target path, not caller path. Always select target canonical bytes; select optional queue/policy/other tickets within existing aggregate budget. Private state and every other intent projection remain strict. Missing canonical target refuses. Implement with an unexported value-receiver reader option passed only by this entrypoint; existing Audit and RequestIndex retain exact behavior. On success report explicit projection qualifier ALL_EXCEPT_TARGET_AGREE; no partial result authorizes a write. Pending remains REDO_PENDING and is rejected by this writer. Test empty/malformed D, unrelated drift, private corruption, pending, wrong scope and snapshot binding.
2. Add store.Reconcile(ctx, repo, actor, ReconcileRequest{RequestID,TargetID,Choice,File,CanonicalSha256}, now). Choice only KEEP_JOURNAL/ADOPT_FILE. Bind queue from parsed target; validate request/digest/actor under existing model rules before effects. File is explicit original offered/discarded bytes supplied by caller, never silently refreshed on retry; KEEP requires original canonical digest. Read under exclusive lock. Existing VERSION/restore/primary guards apply; ALL is exempt only for this explicit reconciliation entrypoint. Refuse pending journal via full validated RequestIndex without redo. No recovery effects on refusal.
3. Validated request lookup → original replay or conflict before fresh projection/branch checks, preserving original TicketID. Reuse small replay-result helper with existing mutation writer if justified; no raw unvalidated request/receipt reads. Fresh inventory → strict except-target full audit selects canonical queue/policy/tickets → actual primary branch → Model existing Keep/Adopt. Offered File must equal current projection by inventory; KEEP canonical hash must match journal. Bind full head/intent snapshot and branch immediately before apply, with ALL exemption confined to this operation. Apply existing §5.2 writer/capacity model. No new acceptance or role rules, no zero-attempt oracle beyond existing no-runtime premise. Preserve discarded bytes for KEEP as existing model requires; ADOPT uses existing fixed field composition/revision rules.
4. Closed CLI: `reconcile intent --target ID (--keep-journal --canonical-sha256 DIGEST | --adopt-file) --file PATH --request-id ID [--role OWNER|OPERATOR]`. --file mandatory and bounded, no-follow file read; '-' may read bounded stdin if included. Explicit input copy makes exact retries reproducible after projection changes; no current-file default that changes digest on retry. Reject invalid/duplicate/inapplicable flags before reads. No payload prose becomes path/argv/policy. Existing mutateResult envelope reports original ID on replay. Help/spec document syntax and stable-input requirement. File path is an explicit operator argument, never queue prose. No inspection mode/new wire profile needed.
5. Tests: full KEEP and ADOPT paths then further mutation; receipts/revisions/discarded evidence; byte-identical replay and different-file/request conflict; protected-field ADOPT refusal; empty D KEEP; ALL exemption with genuinely journalled barrier; ADMISSION; wrong/detached primary branch; unrelated divergence; actor-role refusal; primary/restore/VERSION refusal; pending/staging refusal without redo; offered file differs physical and stale canonical hash; CLI flags/read bounds and exact retry. Every new behavior named TM/AS; no unqualified claim.
6. Focused tests then one frozen final make verify + independent Gate B; same interruption-tested supervisor. Existing Stage1/2 gate evidence retained; only new source invalidates final-tree reuse. Record source manifest and result, update pending canonical-retrieval memory only on verified success.

Practical risks/tradeoffs: explicit original file and KEEP canonical hash are less convenient than implicit current-file reads, but preserve existing frozen request digest and make retries safe without inventing history-based input reconstruction. Settled-only is a deliberate conservative preview limitation: pending receipt recovery remains separate rather than skipping or overwriting third values. No extra filesystem cleanup, new service, daemon, provider call, or Corvint source change.

Gate A implementation refinements (before edits):
- Explicitly refuse StagingPresent on fresh reconciliation, and strengthen fresh mutation/pending redo to refuse a valid active descriptor until recovery is implemented (claim internal/store/redo.go and guards_test.go too). Request replay may remain read-only after validated lookup. Add actual empty-assigned-slot active-descriptor witness.
- intent.TreeDigest deliberately rejects empty ticket D. Reconciliation therefore binds its proof immediately before apply with a second complete Reader.Reconciliation(target) audit, comparing the entire Identity (head, inventory, intent), checking no staging, plus actual branch and guarded primary/marker state. This retains existing global digest/read semantics and supports contracted present-empty D; no new permissive raw reader or partial-result authority. At most two bounded reconciliation audits; no per-ticket re-audits.
- To make the required canonical ticket digest reviewable and obtainable, add pure `reconcile inspect <ticket-id>` returning one item {ticketId, canonicalRecordSha256, canonicalRecord, projectionAgreement, historicalAcceptance, actorAuthentication, liveness, runtimeQualification}. It uses one successful Reader.Reconciliation(target), whose own capture/walk/recapture gives the bounded TM-V0-008 snapshot; native Resolve plus ID-derived queue identity, no intent.Load (which rejects malformed D). Snapshot envelope is built from that successful audit's Head and Identity, partial only on errors, with no item on errors. This read takes no lock, writes nothing, does not use the writer guards/branch requirement, and reports pending/errors verbatim. `canonicalRecord` is untrusted JSON, `untrusted=[UNTRUSTED_QUEUE_DATA]`. It never labels the receipt checksum as a ticket checksum. Optional additional selected paths are not exposed in CLI.
- CLI workflow docs: inspect canonical record/hash, preserve an immutable copy of edited projection outside .taskman, supply that copy and returned ticket-record hash for KEEP; retain both for retries. ADOPT uses the same preserved copy and no canonical hash. Tests cover inspect malformed/empty D, snapshots/no writes, and full inspect→choice→reconcile→retry→ordinary-mutation workflow. No new payload schema beyond item shape; acceptance/revision mechanics unchanged.

Applier delta Gate A PASS: KEEP_JOURNAL's discarded-D blob is already a POST at evidence/<digest>; map it to immutable RoleEvidence and publish it in phase 0 before receipt (same reviewed fixture protocol). Claim internal/store/store.go and export_test.go. Private per-call beforeCommit callback (production nil, test-only export alias) verifies evidence exists before returned commit fault, unchanged projection/head/request/receipts, empty staging and successful orphan reuse on retry. No crash/power-loss claim. Inspector selects the canonical barrier alongside target to preserve snapshot barrier reporting.
