# Corvint orchestration implementation handoff

## Owner request and scope

The owner requested an audit and implementation toward complete LLM task management and smart
orchestration using Corvint, prioritizing safe autonomous execution, Corvint orchestration and
feature completeness equally. After the verified local fixture increments, the owner said
“do it” to the proposed separate Corvint implementation task. That authorizes this task;
do not ask again whether to create it or begin its ordinary implementation work.

Target the Corvint project at `/Users/russelllewis/projects/corvint`, in an isolated worktree
from its configured default branch. Do not reuse another task's checkout or working changes.
Corvint is not currently returned by the Codex saved-project listing; task creation awaits its
addition as a saved project. No implementation task has been created yet.

## Evidence to retain

Read these files in `/Users/russelllewis/projects/corvint-tasks` as integration context:

- `docs/reviews/2026-09-19-functionality-orchestration-audit.md`
- `docs/reviews/2026-09-19-writer-repair.md`
- `docs/SPEC.md`, especially authority, planning, admission, completion and GP requirements
- `docs/decisions/0001-task-control-plane.md` and decisions 0003, 0004, 0007 and 0008
- `docs/ROADMAP.md` and pending `docs/agent-memory` entries

The task-store changes are **uncommitted** atop
`5117b9238f9ccc7caf01bc9a5e0ab1877ba76208`. HEAD alone does not contain the delivered fixes.
The final source binding is `docs/reviews/2026-09-19-writer-evidence/barrier-source-manifest.json`;
the matching canonical `make verify` passed in 138.32 seconds, independent Gate B passed, and
the built executable fixture workflow passed. Do not rerun these unchanged checks merely to
repeat the audit. The report retains preceding-stage evidence and explicit qualification limits.

Delivered fixture behavior: canonical journal authority and writer guards, reliable request
replay identity, receipt audit, explicit KEEP_JOURNAL/ADOPT_FILE reconciliation, and pause/unpause.
No runtime or reservations are implemented. Pending UNPAUSE deletion, interrupted genesis,
staging recovery, archive restore and production durability remain incomplete. No admission,
real-queue operation, Linux runtime qualification or performance result is claimed.

## Implement the Corvint side

Begin with Corvint's current AGENTS.md, SELF-DEVELOPMENT.md and applicable Corvint skill workflow.
Inspect current work and authority before writing; preserve other tasks and their gates. Use
Corvint's bounded evidence packets to find relevant code, then read the current source. Record
stale indexes, omitted evidence and unknown coverage instead of treating packets as proof.

1. Resolve the accepted Corvint-side ATCP/ATM/WQO authority and outstanding amendment records.
   Six exact governing copies referenced by task-store decision 0001 are absent from their old
   temporary directory. A prior local search found no digest matches. Recover verified originals
   if available; never silently substitute current documents. Missing copies block unresolved
   upstream authority or qualification, not unambiguous local fixture implementation.
2. Before any Corvint-facing source edit, freeze the required `taskman-perf-baseline/0` against
   the actual performance harness and honor GP. Preserve its required order and promotion hold;
   no “no slowdown” claim without the required result. If the current accepted Corvint contract
   differs, surface the exact authority conflict before choosing an interpretation.
3. Implement the native queue adapter and operative `taskman-plan/0` planning path (TCP-03).
   Prove a read-only fixture path early: native ticket store plus reservation observations →
   deterministic selected/deferred/blocked reasons. Highest-priority runnable work must not be
   displaced by a larger cheap wave. Keep WQO observation/propose-wave non-operative and
   preserve its existing contract. Use source-bound context, impact and conservative collision
   evidence with explicit incomplete-coverage behavior.
4. Bind plans to canonical queue/policy, ticket acceptance revisions, head, intent and reservation
   identities. Keep stale-plan refusal and atomic admission responsibilities in the executor.
   Do not invent live reservation/runtime capabilities to make integration fixtures appear real.
5. Carry the integration through meaningful tests, required native gates, independent review and
   repair, including the Corvint completion/evidence workflow where applicable. Pin exact CEM/OCM
   profiles before claiming their completion semantics. Produce a concrete dependency handoff for
   executor runtime/reservation/completion work that must remain in corvint-tasks.

This is an implementation request, not another audit-only pass. Continue authorized work until
verified or at a concrete owner/external blocker; identify what remains and why. Do not publish,
cut over a real queue, run real autonomous tasks, modify Beamfall, delete history or silently
amend accepted authority. Real-queue cutover and qualification still require their owner records.

## Execution discipline

Task-fit selection: Astra/high for the cross-repository authority, planning and safety reasoning.
The parent cannot inspect or change a new task's live model setting through the available
creation controls; do not claim the selected model is effective without verification. Apply
current owner model-selection rules for implementation and review.

Use one builder and one independent reviewer, no nested delegation, bounded output and at most
two repair cycles. Read account usage before autonomous work; unknown telemetry or 80% weekly
consumption is a stop, and a reset needs a separate explicit request. Latest parent observation
was 9% weekly consumption. Keep a durable task/commit/evidence checkpoint, review the practical
plan before substantial execution, freeze source and commit during gates, and preserve passed
evidence until an actual source change invalidates it. Long-running owned processes require
signal/exit cleanup and an interruption witness. Do not disturb unrelated full gates.
