# Repair R3 — isolated builder evidence, 2026-09-06

All three findings in `/private/tmp/corvint-tasks-build-20260906/codex-repair-r2-review-verdict.md`
are repaired in this candidate. This is builder evidence; fresh independent review and the
parent's single canonical gate after final freeze remain pending.

Comparison: `/private/tmp/corvint-tasks-build-20260906/codex-repair-r2-review-snapshot`.
Only these files differ (including this new report):

- `internal/archive/export.go`
- `internal/archive/staging_test.go`
- `internal/intent/fifo_test.go`
- `docs/SPEC.md` (TM-V0-022 identity and truthful cleanup clarification)
- `docs/agent-memory/fixes.md` (pending R3 review/gate)
- `docs/reviews/2026-09-06-repair-r3.md` (new)

The other 62 Go sources are byte-identical to the comparison candidate, with no missing Go
sources. Historical reports are byte-identical. No Git operation, network, delegation,
foreign source edit, real queue, `ps`, nested job or `make verify` was used.

## Findings and causal evidence

1. **F1 / TM-V0-008 / TM-V0-022 / AS-36 N4f:** common directories now compare with
   `os.SameFile` over two simultaneously retained no-follow directory roots. Both paths are
   PathText-bounded; opening, stat and close failures refuse. Ordinary outside staging,
   default staging and staging in a distinct external Git authority still succeed.
   `TestTMV0022_AS36_StagingCommonDirectoryCaseAlias` exercised a real case alias on this
   Darwin filesystem: distinct resolver spellings, identical filesystem identity. Before
   repair it accepted staging and delivered 19,456 bytes; after repair it refuses with zero
   stdout and both fixture trees preserved. This witness actually ran, rather than skipping;
   it explicitly skips with NOT_RUN on non-Darwin or case-sensitive filesystems.
   `TestTMV0022_AS36_StagingIdentityInspectionFailsClosed` also passes for missing, regular-file,
   symlink and over-bound identities and a valid distinct external Git authority.

2. **F2 / TM-V0-022 / AS-36:** immediate unlink and associated descriptor-close errors are
   accumulated in export-owned sticky cleanup state. A snapshot retry cannot erase either
   failure or authorize delivery. The descriptor is still retained and unlinked before use
   on the successful path. `TestTMV0022_AS36_StagingUnlinkFailureSurvivesMovement` injects a
   one-shot unlink EIO while moving intent or committing a new fixture head. Before repair,
   both cases returned success after two unlink calls and delivered 19,456/20,480 bytes.
   After repair, both return an error and zero stdout. Additional intent+close and head+close
   cases actually close the descriptor, inject a distinct EBUSY close failure and assert both
   EIO and EBUSY survive; all pass. Exactly one failed-unlink entry remains outside the source
   until fixture cleanup, and exactly one descriptor-close call is observed. SPEC explicitly
   records this possible abandoned entry and the impossibility of retracting already delivered
   bytes when a later cleanup error occurs.

3. **F3 / TM-V0-008 / AS-07:** `fifoWitness` subscribes to parent INT/TERM before launching
   the child, checks cancellation during bounded waits, kills/reaps during deferred cleanup,
   and stops signal notification after cleanup. The controlled helper parent publishes its
   owned child's PID and a separate blocked witness using atomic file replacement. Tests send
   INT/TERM only to that helper parent's PID after the child is observed blocked. They check
   child ESRCH before outer cleanup, so outer rescue cannot falsely satisfy parent reap.
   Temporarily disabling only the helper parent's signal subscription caused both regressions
   to fail: parent wait statuses 2/15 and `helper exited without reaping its blocked FIFO child`.
   The outer fixture killed the leftover children and confirmed disappearance; the temporary
   source change was restored by an EXIT trap. The repaired tests pass with normal helper
   exit status 1 carrying `FIFO parent interrupted` and no surviving child.

   The outer fixture itself subscribes before launching the helper. Its deferred cleanup gives
   the helper a bounded cancellation/reap opportunity, then has bounded fallback cleanup for
   the two known PIDs, child first. Helper assertion/deadline, outer early return/timeout,
   explicit context cancellation, and actual INT/TERM to the outer test-process PID all pass.
   The final nine-case helper suite took 5.811s including package overhead; each signal case
   took about 0.07s. No process-group inference or unbounded worker/pipe-reader goroutine was
   introduced. A stopped-on-return 15s child lifetime timer is backup only; passing parent
   interruption witnesses prove reap long before that deadline. The lifetime timer's expiry
   itself was not separately exercised.

## Focused verification

Observed toolchain: `go version go1.27.0 darwin/arm64`. Every test invocation used:

```sh
GOTOOLCHAIN=local \
GOCACHE=/private/tmp/corvint-tasks-build-20260906/go-cache \
GOMAXPROCS=2 GOFLAGS=-p=2
```

The following focused commands passed (red runs described above intentionally exited 1):

```sh
go test -count=1 -run 'TestTMV0022_AS(36_Staging|36_N4|09_(PartialDelivery|TerminalReadError|ContentAddressedNames|ExportThenVerify)|10_Encoded)' -v ./internal/archive
go test -count=1 -run '^TestTMV0022_AS09_BytesAndSourceError$' -v ./internal/archive
go test -count=1 -run 'TestTMV0008_AS07_(FIFO|ReadBoundedFIFORegression|ReadInRootFIFORegression)' -v ./internal/intent
go test -count=1 -run 'TestTMV0008_AS07_FIFOParentCancellation$' -v ./internal/intent
```

The last command reran the complete nine-case parent suite after atomic PID publication was
added. The earlier FIFO command preserved setup remove/mkfifo failures, release failure,
child INT/TERM/KILL, deadline, legacy blocking/release, and safe/root nonblocking witnesses.
Archive coverage preserves directory swap, staging-file replacement, seek/close error
reporting, source movement, malformed/linked staging, encoded cap/PAX accounting, bytes+EIO
at footer and manifest-body boundaries, valid bytes+EOF, payload metrics and exact 17-byte
partial delivery. `gofmt -l` on the three changed Go files is empty.

## Remaining holds

- Independent review and the parent's canonical gate: pending; no canonical gate ran here.
- Native Linux execution: NOT_RUN; existing pending platform work remains.
- Journal, capacity, runtime, durability, saturation/restore work and real-queue cutover remain
  separate holds. No TCP completion, runtime qualification, GP result or performance claim.
- Optional `/private/tmp/corvint-tasks-build-20260906/codex-repair-r3-steering.md` was absent
  when checked before finalization.
