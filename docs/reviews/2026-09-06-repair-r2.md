# Experimental candidate repair R2 — 2026-09-06

Status: F1/F2/F3 repaired; focused witnesses PASS; canonical supervisor FAILED
(environment); fresh independent review and parent verification PENDING.

Work was confined to `codex-repair-r2`; the R1 review snapshot was read-only. No delegation,
commit, publication, real queue, other checkout or Corvint source change. Historical reports
remain byte-identical. This builder does not supply independent review.

## Changes and causal evidence

- **F1:** `internal/archive/export.go` returns a retained `safeopen.Root` from staging
  validation and compares its identity to the original directory stat. Exclusive creation
  and immediate unlink use that handle; one file descriptor survives write → Seek → Verify
  → Seek → delivery → Close. Retry and final cleanup errors are propagated with the primary
  error/code. No pathname reopen or delivery-time remove remains. Existing encoded write
  bounds, snapshot retries, zero stdout before delivery and counted partial-delivery failure
  remain. This closes ordinary pathname reuse; an FD does not stop an OS owner relocating
  its containing directory.
- **F1 witnesses:** `TestTMV0022_AS36_StagingDirectorySwap` renames the validated outside
  directory and symlinks its old path into the fixture repository, observes the target during
  export and compares the whole repository afterward. `StagingFileReplacement` proves the
  stage is unlinked before verification, replaces its old name after verification, verifies
  delivered bytes and proves cleanup preserves the replacement. `StagingFailuresBeforeDelivery`
  injects unlink EIO and closed descriptors at both seeks, checking zero stdout and retained
  seek/close errors. Existing `TestTMV0022_AS09_PartialDelivery` still accepts exactly 17 bytes
  then fails truthfully; existing stream-cap and snapshot tests remain.
- **F2:** `streamReader` retains the first non-EOF source error independently of tar;
  `verifyLimit` refuses on it before success. Encoded overflow keeps LIMIT_EXCEEDED precedence.
  `TestTMV0022_AS09_BytesAndSourceError` supplies exactly the final 512 trailer bytes + EIO,
  then EOF, and complete manifest-body bytes + EIO. It also proves normal bytes + EOF at the
  exact cap and payload-only public metrics. Existing zero-byte EIO and framing/PAX cap
  witnesses remain. Complete-body EIO already refused in R1; the footer case was the new red.
- **F3:** `internal/intent/fifo_test.go` confines the actual blocking legacy `os.Open` to a
  fixture child, since its open cannot be cancelled safely in a goroutine if FIFO release
  fails. Cleanup is registered before launch and hook restoration before setup. Readiness
  polls completion and a deadline; `Wait4(WNOHANG)` bounds waits without worker or pipe-reader
  goroutines. Release failure kills/reaps the child. No fixture child spawns descendants.
  `ReadBoundedFIFORegression` still observes legacy blocking, releases/joins it and proves
  prompt safe opening; `ReadInRootFIFORegression` still replaces after stat. `FIFOSetupFailure`
  injects Mkfifo ENOSPC and remove EACCES. `FIFOReleaseFailureAndInterruption` and `FIFODeadline`
  exercise release EIO, SIGINT, SIGTERM, SIGKILL and deadline cleanup, checking reaping and ESRCH.
- [Archive causal red](2026-09-06-repair-r2-causal-red.log): old production boundaries with
  observation hooks wrote inside the repository, reopened/delivered the replacement, removed
  that replacement, and accepted footer bytes + EIO. [Focused green](2026-09-06-repair-r2-archive-focused.log)
  passes the final repaired witnesses. The complete archive package also passed (11.407s).
- [FIFO causal red](2026-09-06-repair-r2-fifo-causal-red.log): ablating early completion
  observation made both setup failures hit the bounded five-second readiness timeout. The
  ablation was restored before [focused green](2026-09-06-repair-r2-fifo.log), which passes
  all FIFO scenarios including interruption/deadline cleanup. This is an ablation of the new
  bounded harness, not a claim that the original unbounded test was run to completion.

SPEC TM-V0-022 clarifies retained staging identities and sticky source errors without lowering
repository exclusion. Pending memories record the linked-worktree gap and remaining review.

## Exactly one canonical gate

Executed once, after freezing all 65 Go source SHA-256 hashes and the actual Go binary hash:

```sh
python3 /private/tmp/corvint-tasks-build-20260906/run_job.py /private/tmp/corvint-tasks-build-20260906/codex-repair-r2-verify-config.json
```

The supplied config runs `make -j1 verify`, Go 1.27.0 (`GOTOOLCHAIN=local`),
`GOCACHE=/private/tmp/corvint-tasks-build-20260906/go-cache`, `GOMAXPROCS=2`, `GOFLAGS=-p=2`.
[Gate output](2026-09-06-repair-r2-verify.log) reached `go vet ./...` without diagnostics:
version/source/gofmt gates passed, and every `go test -count=1 ./...` package passed.
The [supervisor exited 1](2026-09-06-repair-r2-supervisor.log): sandbox policy denied its final
`/bin/ps` process snapshot. No child exit JSON was written, so a successful canonical
supervisor completion or captured make exit 0 is **not claimed**. The same environmental
failure affected focused FIFO supervision after those tests returned. No gate rerun occurred.

[Post-gate verification](2026-09-06-repair-r2-verification.json) confirms all
[source and Go binary hashes](2026-09-06-repair-r2-go-sha256.json) match. Only documentation
and result artifacts changed afterward. Parent must resolve supervisor completion evidence
in an environment where its existing cleanup mechanism can execute.

## Unresolved findings and holds

**Confirmed linked-worktree staging gap:** SPEC TM-V0-008 / TM-V0-022 / AS-36 N4f exclude the
repository, not merely its primary checkout. A synthetic linked checkout resolving to the
same common directory is still accepted as staging. The [bounded witness](2026-09-06-repair-r2-linked-gap-witness.txt)
and [failure](2026-09-06-repair-r2-linked-gap.log) are preserved as evidence, not as a green
test endorsing that behavior. No filesystem project scan was used. This separate discovery
repair remains pending as requested; do not claim general outside-repository qualification.

Fresh independent review and parent verification remain required before guarded transfer.
Journal, aggregate capacity/recovery reserve proof, reservations, process runtime, restore and
CLI mutation wiring remain unbuilt. Native Linux execution, worst-case RSS, GP/no-slowdown,
physical durability and owner-authorized real-queue qualification are not established here.
No TCP-01/TCP-02 completion or runtime/capacity/GP qualification is claimed.
