# Experimental candidate repair R1 — 2026-09-06

Status: CODE REPAIRED; canonical gate PASSED; fresh independent review PENDING.
The owner answered “you can do either” on 2026-09-06, authorizing Codex continuation after
Claude reached its monthly cap. This report records implementation and executed witnesses;
it does not amend owner authority or qualify a complete runtime. The earlier
[experimental review](2026-09-06-experimental-candidate.md) remains immutable history.

## Scope and evidence

The candidate is `/private/tmp/corvint-tasks-build-20260906/codex-repair-r1`. Original
source comparison: the frozen `combined-review-snapshot` beside it. No Corvint/main/sibling
source, real queue, external service, commit, or publication was changed.

| Finding | Repair and named witnesses |
|---|---|
| A1 | Explicit Darwin `F_FULLFSYNC` under `RawConn.Control`, retrying only EINTR; directory `fsync` stays separate. `TestTMV0010_AS10_DarwinStrictFullSyncBoundary` injects below the actual wrapper, checks two calls for EINTR→ENOTSUP and proves no temp or destination survives. Existing qualification/publication failure tests now inject the correct separate boundary. |
| A2 | Shared Linux magic 0xEF53 is reported `ext2/ext3/ext4`, ambiguous and refused. `TestTMV0010_AS10_AmbiguousExtMagic` checks shared ext2/ext3/ext4 observations cannot qualify and distinct allowed magics remain recognized. Ext4 policy is preserved; descriptor-bound disambiguation is not implemented, so no ext4 mount is promoted by magic alone. |
| A3 | Narrow `internal/safeopen` walks directories with atomic no-follow, directory-only `openat`; final files are nonblocking and no-follow. Authority retains intended directory identity and never reopens the arbitrary path for publication. `TestTMV0001_AS10_AuthorityDirectoryReplacement`, `DirectorySwapNoRedirectOrBlock`, `PinnedRootSurvivesPathReplacement`, `DescriptorBridgeFailsClosed`, `DescriptorLifetime` and `TestTMV0008_AS07_ReadReplacementFIFOAndSymlink` cover API integration, leaf/ancestor swaps, retained identity, bridge refusal and descriptor lifetime. |
| A4 | Every flock attempt checks cancellation/deadline; successful acquisition and return are checked too, and refusal closes the file. `TestTMV0009_AS10_LockInterruptedDeadlineAndCancellation` covers EINTR through timeout, cancellation on EINTR, cancellation on acquisition, and successful subsequent acquisition. |
| A5 | Unsupported entry points refuse before creating locks or publication/probe temps, including a stale publication handle. `TestTMV0010_AS10_UnsupportedPlatformBeforeEffects` proves the whole fixture tree unchanged on the host seam and on an actual unsupported `js/wasm` target under Node. |
| M1 | Both input validation and replay reject typed-nil request indexes. `TestTMV0006_AS03_TypedNilIndexRequired` exercises Apply and Adopt; existing required-input coverage stays. |
| R1 | Gate/ticket callback state resets on every invocation. `TestTMV0008_AS36_ShowRetriesDiscardPriorAttempt` deterministically removes a gate or adds a ticket after the first callback, then checks the stable retry for gate show, ticket show and blockers. |
| R2 | Evidence/pinned basenames must be exact lowercase Digests matching consumed-byte hashes independently of manifest claims. `TestTMV0022_AS09_ContentAddressedNames` covers positives, mismatches, uppercase, nonhex, short, suffix and nested names in verify and export; positive fixtures now use real content identities. |
| R3 | Verification requires actual EOF after the two end blocks; EIO is reported. `TestTMV0022_AS09_TerminalReadError` supplies EIO after a valid stream. |
| R4 | Zero stdout bytes is scoped to failures before delivery. `TestTMV0022_AS09_PartialDelivery` accepts exactly 17 bytes then errors, checks truthful error output and removed staging, and proves both store trees untouched. No stdout rollback is attempted. |
| R5 (parent-confirmed during repair) | The 64 GiB budget counts actual encoded tar bytes including manifest, PAX, headers, padding and trailer. Staging refuses before an over-limit write; verification bounds consumed bytes before retained allocation. `TestTMV0022_AS10_EncodedStreamLimit` and `EncodedLimitIncludesPAX` cover reduced internal caps, payload-only false passes, exact-limit acceptance and framing overflow. Existing public `Bytes` remains payload-only and is documented separately. |
| T1 | Protected completion adoption uses a structurally valid COMPLETED record and proves the modified record decodes. The invalid OPEN-with-completion case remains a MALFORMED negative in `TestTMV0007_AS35_ProtectedFieldsAndIdentity`; protected-field refusal coverage remains. SPEC prose now distinguishes COMPLETED transitions from permitted same-status edits. |

## Executed verification

- Initial focused baseline reproduced T1, M1, A4, R3 and delivery-reporting failures.
- [Causal red run](2026-09-06-repair-r1-causal-red.log): disabling the corresponding
  repaired boundaries produced failures for A1/A2/A3/A5/R1/R2/R3/R4/R5. The temporary
  ablations were restored byte-for-byte before further work.
- [Input/lock red run](2026-09-06-repair-r1-input-lock-red.log): T1's original invalid
  protected-field test, typed-nil acceptance and interrupted/cancelled lock failures
  reproduced. The typed-nil scenario was subsequently given its own TM-V0-006/AS-03 name.
- Restored-source focused run PASSED across authority, safeopen, mutation, intent, archive
  and CLI. The controlled FIFO witness runs the original `os.Open`, observes the block,
  releases it and joins it; it then proves the safe boundary returns promptly. The store
  `readInRoot` witness replaces a file with a FIFO after `statRegular`. These are executed
  findings, superseding the older report's untested read-FIFO hypothesis. Helpers are
  in-process and joined; they spawn no descendants.
- Pinned source inspected: Go 1.27.0 `internal/poll/fd_fsync_darwin.go` contains the ENOTSUP
  fallback; `os/root_unix.go` opens before type-checking and follows internal symlinks;
  `syscall/asm_darwin_arm64.s` supplies the raw syscall ABI. Darwin `openat` uses ABI number
  463 (also checked by the parent against the local Darwin SDK). Host wrapper/swap tests
  execute that syscall. The `/dev/fd/<fd>` bridge is checked for identity under a pinned
  descriptor and refuses if unavailable or different; availability is not assumed uniformly.
- Linux/amd64 authority and safeopen tests, and Windows/amd64 authority tests cross-compiled;
  their native runtime execution is NOT_RUN. Linux classification's pure boundary executed
  on Darwin. No Linux filesystem mount was qualified.
- [Unsupported-target execution](2026-09-06-repair-r1-unsupported-js.log) PASSED using
  GOOS=js GOARCH=wasm and the pinned Go Node runner with GOMAXPROCS=1. The first attempt
  with GOMAXPROCS=2 failed during Go's wasm runtime initialization (`newosproc: not
  implemented`), before the test; the rerun uses wasm's single-thread requirement. This
  exercises the actual unsupported-platform implementation and unchanged-tree witness.
- [Final canonical gate](2026-09-06-repair-r1-verify.log): PASSED, exit 0, no signal,
  2026-09-06. Exactly one `make -j1 verify` ran through the requested cleanup-tested
  `run_claude.py` supervisor, pinned Go 1.27.0, GOMAXPROCS=2, GOFLAGS=-p=2 and temporary
  Go cache. Toolchain, source-existence, gofmt, `go test -count=1 ./...` and `go vet ./...`
  passed. SHA-256 hashes of every Go source file match the pre-gate capture after completion.
  Only documentation/result logs changed afterward. No independent review is claimed by
  this builder.


## Remaining holds

Fresh independent review is required on these repaired bytes. Journal, aggregate capacity
escrow, reservations, process runtime, restore and CLI mutation wiring remain unbuilt.
The whole-store plus terminal/recovery/admin reserve proof still blocks every durable-writer
promotion. Worst-case flat-manifest RSS, GP/no-slowdown, real cutover and runtime durability
qualification remain NOT_RUN. Strict syscall tests and a host test gate are not physical-media
or whole-runtime qualification. No TCP-01/TCP-02 completion checkmark is added.
