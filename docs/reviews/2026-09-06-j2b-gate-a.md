# J2b independent Gate A — PASS with selected descriptor-temp rule frozen

Date: 2026-09-06. Reviewer: native_authority_audit. Scope: pure experimental immutable-input command/plan, inventory/cost and interrupted-state classification only. No filesystem writer, CLI mutation, admission grant, runtime, migration or upstream acceptance. J3 remains held. No tests, full gate, candidate edits or delegation were performed; this report is the only file written.

Reviewed `tcp02-j2-codec-freeze-addendum.md` over `tcp02-j2-capacity-plan-r2.md`, and the relevant original codecs/reducer/audit witness in `tcp02-j1-final`. The parent’s selected descriptor-less-temp correction below is part of this verdict. J2a remains supplied-manifest encoding cost only; inventory/delta/prefix modeling belongs to J2b. The preserved hard caps are 1,000,000 receipts and 4 GiB, with UNPAUSE reserved inside them; no extension is permitted.

## Finding resolved by the parent’s explicit selection

**MED — classify the descriptor-less temp prefix separately.** Addendum lines 76 and 104 describe interrupted artifacts and descriptor-owned abandonment; inherited r2 lines 61 and 65 also admit the earlier prefix that has only `active.json.tmp`. A crash while writing that first descriptor temp under an existing barrier leaves no valid `active.json`, so descriptor-owned abort alone cannot release this prefix before UNPAUSE. Requiring the partial descriptor to parse would strand a legitimate interrupted preparation.

The parent selected this exact pure-model amendment:

- Classify `TempPreparationAbortable` only with a stable unchanged base, complete receipt inventory showing no next/extra receipt, zero attempts/effects, absent `active.json`, no artifact slots or unknown entries, and only the fixed regular `active.json.tmp` whose observed length is at most 2,422 bytes. An empty staging directory is the corresponding empty preparation state.
- Do not require these bounded temp bytes to parse, including full-length malformed bytes. With no published active descriptor they never became authority, and the prescribed ordering forbids payload production before that descriptor is published. Do not infer a completed descriptor from its length. Malformed `active.json`, extra artifact slots, unknown entries or failed state/identity conditions remain holds/refusals; this exception does not generalize to them.
- Model unlink of only that fixed temp, followed by staging-directory fsync, before starting any new descriptor. Keep its D/Pb cost through the durability boundary. This branch allocates no new artifact, receipt or evidence and deletes no history. Existing linked orphans remain charged. The initial unknown operation uses the global 2,422-byte temp cap, not the smaller UNPAUSE descriptor cap.
- J3 must establish the sole-writer/type/identity/ownership and durable-order premises. The J2b classification is a conditional model result, not permission to unlink a real path.

Required pure witnesses: zero-length, partial JSON, complete malformed and cap-sized temp-only states classify consistently; cap+1, symlink/nonregular metadata, an active descriptor, any payload slot/unknown entry, changed base or next/extra receipt refuse; the temp remains charged before modeled fsync and a new plan cannot start early. The temp-only abort-to-UNPAUSE path must be included in capacity witnesses.

No remaining HIGH/MED finding after this selection.

## Evidence supporting the bounded PASS

- The administrative preimages are closed and stable across retries; KEEP_JOURNAL binds original canonical C and physical D explicitly, while ADOPT_FILE preserves `mutation.AdoptDigest` unchanged (`internal/mutation/adopt.go:33–59`). Replay precedes fresh transition preconditions; NoChange/refusal creates no receipt/index/artifact; empty accepted ADOPT composition retains the reducer’s revision increment (`adopt.go:107–108,138–160`). Supplied actors and modeled policy decisions do not establish trusted authorization.
- Physical preconditions and canonical predecessors are correctly distinct. `snapshot/receipt.go:122–139` pairs physical pre/post paths without imposing the previous canonical digest, and `journal/audit_test.go:818–854` provides the explicit D-versus-C witness. KEEP restores C unchanged and retains even empty raw D; ADOPT chains C′ from C through the existing reducer. Historical authorization remains NOT_OBSERVED.
- Independent canonical ASCII length arithmetic reproduces all descriptor caps exactly: INIT 2,422; PAUSE 1,243; UNPAUSE 1,098; KEEP_JOURNAL 1,676; ADOPT_FILE 1,467. The queue/local compatibility constraint and per-operation paths avoid impossible independent maxima. The eleven-slot maximum and evidence-POST deduplication are internally consistent. These are analytical checks, not execution of the future descriptor codec; the proposed codec parity/maximizing witnesses remain mandatory.
- The four codec source hashes in `j2-unpause-codec-parity.json` match `tcp02-j1-final`. Its receipt/index/outcome lengths 1,683/563/308 therefore remain applicable to the inspected codec sources. The probe’s sequence 1,000,001 is only a seven-digit encoding witness; it gives no capacity permission. The selected hard-cap rules refuse ordinary work earlier to preserve closure.
- The model explicitly retains delayed barrier deletion, the new receipt/index peak, a missing request shard, staged head/descriptor costs, every linked orphan and the old projection on abort. No abandoned shrink is credited. UNPAUSE’s three artifact bounds total 6,342 bytes and two 1,098-byte descriptors produce the stated conservative Pb peak of 8,538 bytes. D counts names/directories independently of archive F; Pb is logical temporary charging, not physical allocation or ENOSPC immunity.
- Descriptor-owned cleanup removes payloads/temp and syncs before removing/syncing the descriptor; new transactions cannot overlap ownership eras. The selected temp-only rule closes the earlier prefix. Committed pending UNPAUSE reuses its receipt; fresh requests cannot mint another reserve. Broader saturated or unsupported states receive no retroactive writer eligibility.

## Reviewed document identity

- `tcp02-j2-codec-freeze-addendum.md`: SHA-256 `b6f63c935cda8fc5395f48d961a797e8071637ee478a3e7e73d611d5b563f557` before incorporation of the selected temp-only rule.
- `tcp02-j2-capacity-plan-r2.md`: SHA-256 `28f987d8e2357e25b0771aa1807c98cc096b47d20e638ae542fb2ae4ed38c976`.

This is plan clearance for the described pure J2b model, with the selected correction incorporated before builder handoff. It is not implementation, test, admission, authorization or durability qualification.

## Selected administrative-policy API clarification — PASS

The parent’s proposed clarification is sound and is part of the builder freeze. `tcp02-j1-final/internal/intent/policy.go:13–17,28–37` defines a ticket-operation matrix, not administrative authorization. Feeding INIT/PAUSE/UNPAUSE/KEEP_JOURNAL into that matrix would always refuse; adding administrative rows would invent a new policy contract.

For J2b, administrative templates accept an explicitly supplied, well-formed **hypothetical** OWNER/OPERATOR actor binding solely for deterministic digest/receipt/cost computation. Every such result reports `ActorAuthentication=NOT_OBSERVED` and `AdministrativeAuthorization=NOT_OBSERVED`, including modeled transaction, replay and no-change classifications. A role outside this modeled subset can be structurally refused, but accepting the role is not successful authentication or administrative authorization. The existing phrase “after permission checks” must not cause an invented policy lookup: it means the model’s declared shape/scope/barrier checks; real permission checks remain a J3 prerequisite.

Do not add an administrative authorization codec, policy row, caller-controlled authorization flag, or boolean that defaults to allowing writes. No model output grants execution. Existing ticket-policy restrictions remain enforced by `mutation.Adopt` composition with its current semantics; the clarification does not bypass or change those checks. J3 must independently obtain trusted actor binding and applicable administrative authorization, apply upstream/pinned restrictions, and cannot consume the pure result alone as permission. This replaces the addendum’s ambiguous policy-removal language only for the administrative pure templates; it does not widen the ticket policy matrix or accept an upstream grant.

No new HIGH/MED remains after this clarification. No source edits or tests were performed for it.

## Shared staging codec ownership — PASS

Independent bounded layering review inspected the J2b draft `internal/transaction/stage.go`, `capacity.go` and current snapshot imports. `transaction/capacity.go` imports archive for `MeasureManifestEncoding`; archive already imports snapshot. A future archive import of transaction for `DecodeDescriptor` would therefore form `archive -> transaction -> archive`.

Move only the closed `taskman-stage/0` descriptor/base/entry types, canonical codec, profile-owned limits/vocabulary and structural shape validation into new `internal/snapshot/stage.go` with new codec tests. Snapshot already owns the shared receipt/head/request codecs and has no archive or transaction dependency. The intended graph remains `transaction -> archive -> snapshot` plus `transaction -> snapshot`. This is the smallest appropriate ownership correction; a new package is unnecessary on the observed dependencies.

Transaction aliases/reuses those exact types and decoder; retain owned artifact bytes/cloning, plan matching, hypothetical inventory interpretation, stage classification, cleanup sequencing and capacity calculations in transaction. Do not pull `Plan`, `Inventory`, `Coverage`, administrative-policy claims or any filesystem operation into snapshot. Any operation names/size or base-sequence constants needed by descriptor validation must be owned below transaction and referenced/aliased upward, never supplied by importing transaction back into snapshot. Keep descriptor-specific hard bounds distinct from a decision that capacity or reserves are available.

Preserve canonical bytes, ordering, operation caps, target/queue/request relationships, limits and existing negative classifications during the move. There should be one implementation of descriptor validation, not a reduced archive decoder beside a stronger transaction decoder. Keep the new codec test dependencies cycle-free: use direct descriptor fixtures or external `snapshot_test` tests rather than importing transaction into an in-package snapshot test. No existing snapshot source changes or archive/journal staging-layout activation are needed for this relocation.

No HIGH/MED concern remains in the proposed ownership change. This is layering clearance only; decoding a descriptor is not plan coherence, inventory completeness, cleanup ownership or permission to execute. No candidate source was modified and no tests were run for this review.
