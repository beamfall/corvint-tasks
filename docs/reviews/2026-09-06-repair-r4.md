# Repair R4 — isolated FIFO harness evidence, 2026-09-06

The verified stale numeric child-PID fallback in
`/private/tmp/corvint-tasks-build-20260906/codex-repair-r3-review-verdict.md` is repaired in
this candidate. F1, F2 and ordinary parent interruption are archived PASS from that verdict.
F1/F2 sources were not changed, and ordinary parent interruption logic was preserved rather
than re-audited. This is builder evidence only. Fresh independent review and the parent's
canonical gate remain pending.

Only these claimed files were changed or added:

- `internal/intent/fifo_test.go`
- `docs/agent-memory/fixes.md`
- `docs/reviews/2026-09-06-repair-r4.md`

All entries in `docs/reviews/codex-repair-r3-frozen-sha256.json` outside the two claimed
pre-existing files still match their frozen SHA-256 values. Historical reports are therefore
byte-identical. No Git operation, network, delegation, foreign source edit, production change,
real queue or `make verify` was used.

## Exact repair and evidence

1. The outer fixture no longer sends any destructive signal to the child's published numeric
   PID. `awaitPublishedChildExpiry` uses only signal 0 to observe disappearance. Its cleanup
   waits `fifoChildLifetime + 5s` (20s), accommodating the child's actual 15s independent
   lifetime. The outer fixture may still terminate and reap its directly owned helper parent.

2. Destructive child kill/reap remains unchanged in `fifoWitness`, the actual parent while its
   child is unreaped. The full focused intent run preserved FIFO setup/release failures, direct
   child INT/TERM/KILL, FIFO deadline, safe/root opens, helper assertion/deadline, helper
   INT/TERM, outer return/timeout/cancel and outer INT/TERM. All passed. In the causal helper
   INT/TERM cases, the helper exited with the expected failure record and the child was already
   absent before outer cleanup; outer interruption cases also completed cleanup before the
   signal subscription stopped.

3. `TestTMV0008_AS07_FIFOStaleChildPIDIsDiagnosticOnly` injects a controlled live unrelated
   identity and a signal recorder. The bounded fallback observes that identity through the
   configured 20s budget using an injected clock, reports it not gone, and records only signal
   0. The focused witness passed in 0.00s; no destructive signal was requested.

4. The `expiry` parent witness kills and reaps only the directly owned helper, deliberately
   orphaning its blocked FIFO child. No outer child signal is sent. The child exited at its
   independent 15s lifetime and the witness observed the published PID gone. Observed evidence:
   helper PID 37085 reaped, child PID 37086 gone, reason `child lifetime expired`; the isolated
   witness passed in 15.01s. The later full focused run independently passed the same path in
   15.01s with helper PID 39655 and child PID 39656.

## Focused verification

Observed toolchain: `go version go1.27.0 darwin/arm64`. Every Go invocation used:

```sh
GOTOOLCHAIN=local \
GOCACHE=/private/tmp/corvint-tasks-build-20260906/go-cache \
GOMAXPROCS=2 GOFLAGS=-p=2
```

These focused commands passed:

```sh
go test -count=1 ./internal/intent -run 'TestTMV0008_AS07_FIFOStaleChildPIDIsDiagnosticOnly$' -v
go test -count=1 ./internal/intent -run 'TestTMV0008_AS07_FIFOParentCancellation/expiry$' -v
go test -count=1 ./internal/intent -v
```

The complete focused package run passed in 22.184s. The ten parent cases took 20.61s: ordinary
helper and outer interruption cases completed in about 0.06–0.08s, the controlled parent
deadline in 5.06s, and orphan expiry in 15.01s. `gofmt` was applied to the sole changed Go file.

## Remaining holds

- Parent `make verify` and fresh independent review: pending; no canonical gate ran here.
- Native Linux execution: NOT_RUN.
- Journal, capacity, runtime, durable-writer, saturation/restore, GP and real-queue cutover remain
  separate holds. No runtime qualification, GP result, TCP completion or performance claim.
- Optional `/private/tmp/corvint-tasks-build-20260906/codex-repair-r4-steering.md` was absent when
  checked before finalization.
