**REPAIR — exact candidate is not cleared to save as experimental fixture support.**

1. **HIGH — reserved UNPAUSE rejects the required abort state.** [internal/transaction/model.go:470](/private/tmp/corvint-tasks-build-20260906/tcp02-j2b-review-snapshot/internal/transaction/model.go:470) rejects every divergent ticket when UNPAUSE has no target. Counterexample: abort the KEEP fixture from `capacity_test.go:237` after durable cleanup, retaining canonical C, physical D and linked evidence. `AbortCapacity` reports an UNPAUSE envelope, but fresh `Model(UNPAUSE, …)` returns `VALIDATION_FAILED/MALFORMED` because D differs from C. This violates the addendum §5 successful abort-to-UNPAUSE requirement and SPEC §5.5.6. **Smallest fix:** exempt UNPAUSE from unrelated ticket projection-equality checks while preserving inventory validation; extend abort witnesses to invoke the actual fresh UNPAUSE model with C/D unchanged.

2. **MED — cross-operation request conflicts receive the wrong outcome.** [internal/transaction/model.go:261](/private/tmp/corvint-tasks-build-20260906/tcp02-j2b-review-snapshot/internal/transaction/model.go:261) checks the stored outcome against the incoming operation before comparing digests at line 267. Counterexample: reuse a committed PAUSE request ID for a well-formed KEEP request, supplying the original FOUND index. PAUSE’s null revisions trigger `VALIDATION_FAILED/MALFORMED`; TM-V0-006 and addendum §1 require `REQUEST_ID_CONFLICT`. **Smallest fix:** compare digests after generic index/identity validation and before incoming-operation outcome checks; cover administrative-to-reconciliation conflicts in both directions.

Static review only; supplied parent gate accepted. No tests, writes, delegation or Git mutations performed.
