Two MED preliminary findings, both **unexecuted static hypotheses**:

1. **Stale-plan validation omits the original inventory** — [fixture_transaction_test.go:394](/private/tmp/corvint-tasks-build-20260906/tcp02-j3-fixture-preliminary-review/internal/authority/fixture_transaction_test.go:394). Only head identity and affected preimages are compared. Counterexample: include an unreferenced orphan when modeling PAUSE, then remove it before `publish`. Observation still validates, head/preimages match, and reduced costs fit the old envelopes. The precommit comparison uses the newly captured inventory, so publication can commit; exact final equality detects the missing orphan only after cleanup. This violates refusal before preparation for a stale inventory. **Smallest test-only repair:** retain the exact inventory observed during modeling, bind it to that Plan, and compare before issuing the era or creating artifacts. Add this unchanged-head orphan-removal witness, asserting no new artifacts.

2. **Wrong-branch witness uses an incompatible stage token** — [fixture_transaction_fault_test.go:482](/private/tmp/corvint-tasks-build-20260906/tcp02-j3-fixture-preliminary-review/internal/authority/fixture_transaction_fault_test.go:482). The test selects `a00`, which is the HEAD artifact for this KEEP fixture, then passes it to `replaceTicket`. Removing the helper’s branch guard would still leave the test passing: `source` independently rejects the HEAD-to-ticket role mismatch. The assertion therefore does not establish branch enforcement. **Smallest test-only repair:** select the artifact targeting `ftTicket`, prepare through its slot, and use that token with the exact physical preimage. Include an equivalent valid-branch control at a committed prefix.

All existing files match the accepted baseline; the three file hashes match the builder report. No tests, edits, delegation or Git mutations were performed. The reported 17 suites/333 passes remain focused evidence only. All five native integration groups, the combined canonical gate and fresh final independent review remain pending; this review grants no saving or merge clearance.
