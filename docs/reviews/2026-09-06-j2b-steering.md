# Parent J2b steering — apply before final report

1. **Post-before-head phase order.** The current capacity.go draft records `durable-head` before `durable-barrier-unlink`. That is a useful conservative size envelope but is not a valid recoverable durable publication order: a crash with head already advanced but its barrier deletion unapplied is no longer J1's linked-next-receipt REDO_PENDING state. Model every required post (including UNPAUSE deletion) before head publication. You may separately retain the old barrier's charged cost for an upper-bound peak, but name that as a conservative charge/envelope, not as an actual durable publication point. Required regression: the modeled operation order never advances head ahead of a required post/deletion, while the retained-barrier peak still includes the new receipt/index and head replacement budget. This matches the original transaction contract; it is not a new writer feature.

2. **Shared codec layering (independent Gate A PASS).** The future archive staging reader must reuse the descriptor codec, but `transaction` imports `archive` for costs, so `archive -> transaction` would cycle. Authorize NEW `internal/snapshot/stage.go` and `stage_test.go` as the minimal shared read-only codec home. Move only descriptor/base/description types, canonical encode/decode and structural validation there; transaction aliases/reuses them and keeps payload Artifact, plan matching, stage classification and capacity. Alias shared codec constants upward; keep a single decoder and identical bytes/validation. No snapshot -> transaction import, including in-package tests; move codec maximizing tests with their decoder or use external tests appropriately. No existing snapshot source changes or reader-layout activation. Focused snapshot tests are now authorized alongside transaction tests; parent still owns full gate. Independent clarification appended to tcp02-j2b-gate-a.md.

3. **Owner delivery scope.** The owner explicitly selected “Keep this delivery fixture-only” on 2026-09-06. Parent-owned docs/decisions/0002-fixture-only-first-delivery.md now records it in this worktree; do not overwrite it. Continue pure model scope and preserve all NOT_OBSERVED boundaries. This is actual scope selection, not real issuer or runtime qualification, and not default-branch merge approval.
