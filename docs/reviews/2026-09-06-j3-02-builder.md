# J3-02 bounded read-only staging — builder evidence, 2026-09-06

Status: candidate implemented with focused Darwin/arm64 regressions passing. Parent source
freeze, one canonical `make verify`, and fresh independent exact-diff review remain PENDING.
TCP-02 is incomplete. This report is builder evidence, not independent review, merge approval,
full transaction acceptance, cleanup authority, durable-writer or runtime qualification.

## Scope and inputs

Supplied baseline HEAD: `27460b6caa8a760344d15fc160d6effbb9de7a54` (accepted J2b repair plus
J3-01 fixture primitives). Read AGENTS and all four referenced conventions, SPEC §§1–7,
decision 0002, the whole J3-02 plan, selected amendments and Gate A. The later selected
completed-operation/timestamp and receipt-only genesis corrections and optional steering were
read and applied.

Input hashes after that selection:

- Plan `/private/tmp/corvint-tasks-build-20260906/tcp02-j3-02-read-layout-plan.md`:
  `a576a27e839012e92cb6682acd40c29b02c688aab10e4bfb26a3f4cbe045521b`.
- Gate A `/private/tmp/corvint-tasks-build-20260906/tcp02-j3-02-gate-a.md`:
  `5f186099959c5e4e4d5f2deeb294988ef15b85590703c672506c2c76541be146`.
- Steering `/private/tmp/corvint-tasks-build-20260906/tcp02-j3-02-steering.md`:
  `47410fe1b50cc8bfbe8d038cf7c5c584dde15c3e33a240a92f1c1a1364873bb5`.

Exact Go ownership used:

- New `internal/snapshot/stage_observation.go`, `stage_observation_test.go`.
- `internal/archive/layout.go`, `export.go`; new `stage_read.go`, `stage_read_test.go`.
- `internal/journal/audit.go`, `records.go`, `source.go`, directly relevant `audit_test.go`;
  new `stage_read.go`, `stage_read_test.go`.

Documentation: SPEC §5.6 and staging layout cross-reference; ROADMAP/BUILD-LOG candidate status;
pending-only tests/fixes memory; this report. No ownership conflict or blocking dependency
was encountered. Source.List now returns explicit `Listing{DirectoryInfo,Entries}`. Existing
Source decorators embed the interface and inherit that result; no outside adapter edit was
compiler-required. The existing state-budget witness excludes newly observed root identities
from D. Transaction and CLI package compile-only checks also passed.

Protected source check: `git diff --exit-code HEAD -- internal/transaction internal/authority
internal/safeopen internal/snapshot/probe.go internal/snapshot/stage.go
internal/snapshot/stage_test.go internal/wire internal/archive/cost.go
internal/archive/manifest.go` produced no diff. Shared J2b/J3-01 sources remain byte-identical.
No delegation, Git staging/commit/merge, main edit, canonical gate, broad project gate, CLI
mutation, real queue, publication, server, test database or child-process harness was used.

## Delivered mechanics and evidence boundary

One pure structural observer reuses the frozen codec. Closed thirteen-child staging plus its
directory consumes scan D, never archive payload F. Empty staging is inactive; bounded arbitrary
temp-only bytes remain permitted. Active/temp actual-length equality, descriptor-derived slot
bounds, partial/full/empty bytes and exact digest failures are enforced. Only descriptor/digest
metadata survives validation. All linked evidence/pinned orphans remain in the inventory/export.

Native attempts retain/reuse no-follow root and parent handles. Explicit directory identity,
entry metadata and consumed staging hashes are rechecked, including empty roots, detached
named bindings and restored-mtime edits. Observed disappearance earns movement; initial optional
absence and unrelated I/O do not. Close errors are joined and survive movement. Synthetic
Sources supply explicit observations without a native-host claim; there is no optional
verification fallback or public snapshot.Reader change.

Journal uses one four-attempt loop. Archive owns four total attempts and forces the inner
Reader to `Retries:-1`. Stable body errors remain errors; changed complete tuples retry;
failed recapture refuses. Header pending/fork re-probes retain precedence. Attempt outputs,
manifests, files and stream state reset. External archive cleanup and stream-cap errors are
sticky even when a later attempt would succeed. Verification precedes stdout; existing
partial-delivery wording remains. The legacy UNINITIALIZED-remnant indicator is preserved,
excluding the newly observed roots and an otherwise empty staging directory.

Completed leftovers consume actual head/receipt/request bytes, including retained request
blobs, matching artifact digests/lengths, sequence/prev/generation/request/outcome and original
request digest. Parent-selected operation/kind mapping and exact recordedAt equality apply:
INIT→INIT, PAUSE→PAUSE, UNPAUSE→UNPAUSE, KEEP_JOURNAL/ADOPT_FILE→RECONCILE. This does not distinguish
historical KEEP versus ADOPT acceptance or reconstruct a full plan. Precommit requires no
future receipt. Missing head/pending genesis/next/fork distinctions remain with the readers.

## Causal failures, repairs and rejected assumptions

1. Initial `go test ./internal/journal -run TestTMV0006_AS35_EmptyTicketRequestLookup -count=1`
   failed both corruption subfixtures at recorded lookup with
   `MALFORMED: intent/tickets/A.json: empty intent file`. The early physical-ticket size check
   was the cause. The repair permits present-empty ticket capture/hash only; strict Audit
   returns INTENT_DIVERGED and request-only lookup verifies FOUND/ABSENT with intent agreement
   NOT_OBSERVED. Missing/corrupt request projections and actual read errors still refuse.
   A final causal overlay restored **only** the old size predicate in an external copy of
   current audit.go; both recorded and absent lookups then failed MALFORMED, and strict Audit
   failed its expected INTENT_DIVERGED check. Current sources were never rolled back.
2. The first selected archive layout regression failed despite printing LIMIT_EXCEEDED:
   `errors.Join(primary,nil)` masked the concrete wire error type used by CodeOf. The close
   boundary now returns primary unchanged when cleanup succeeds, and joins/wraps actual close
   failures. Repeating `ExportThenVerify|ExportEntryBoundDuringListing|
   StagingUnlinkFailureSurvivesMovement` passed. No wire/code API change was made.
3. Parent-selected completed-stage correction had causal red in
   `TestTMV0002_AS10_CompletedStageOperationAndTimestamp`: UNPAUSE-labelled PAUSE, reverse
   PAUSE-labelled UNPAUSE, KEEP-labelled PAUSE and descriptor-only timestamp edits all returned
   nil incorrectly. Matching all five mappings already passed. Adding the current-byte
   operation/kind and timestamp comparison made all cases green. Native negatives use an
   actual linked PAUSE receipt/head/request and preserve source bytes with zero archive stdout.
   Earlier positive native fixtures used generic MUTATION receipts and a different descriptor
   timestamp; they were corrected to matching UNPAUSE receipts/timestamps, not weakened.
4. A new fixture-only test initially did not compile because Queue exposes Value(), not Encode().
   The fixture now uses `wire.EncodeFile(q.Value())`; the focused restriction tests passed.

5. The final headless-genesis witness initially failed JOURNAL_FORKED because new staging
   scope validation required physical queue.json before INIT projection. Parent independently
   selected the same regression and required binding after the complete existing genesis
   audit. The final implementation retains the bounded queue afterimage from postBytes and
   validateRecord, and defers headless stage binding until chain/blob/request/projection
   checks succeed. It uses those verified bytes only on an actual physical-queue absence;
   present invalid bytes and actual read errors never select that branch. The overlay red
   restores only the physical-queue requirement and fails the actual receipt-only active INIT
   witness. Green covers no materialized posts, partial projection, missing/corrupt blob,
   malformed/wrong-scope/nonfixture queue, invalid genesis, foreign descriptor, physical
   divergence, read errors (including the later binding read), extra/gapped receipts and
   no linked receipt. The combined nonfixture+missing-blob case proves full genesis failure
   precedes queue-dependent staging restriction. No staged queue slot supplies proof.

No remaining confirmed implementation failure was observed in the focused runs. These are
bounded test results, not proof that independent review will find no issue.

## Executed commands and finite witnesses

All Go commands used Go 1.27.0, Darwin/arm64, with:

```sh
GOTOOLCHAIN=local
GOCACHE=/private/tmp/corvint-tasks-build-20260906/go-cache
GOMAXPROCS=2
GOFLAGS=-p=2
```

The command prefixes above apply to every command below. Logs live under
`/private/tmp/corvint-tasks-build-20260906/` for the parent.

- `go test -count=1 -v ./internal/snapshot ./internal/journal` — PASS;
  `tcp02-j3-02-focused-snapshot-journal.log`. Covers existing codec, chain, request, blob,
  projection, streaming, bounds and read-purity regressions alongside the then-current staging tests.
- `go test -count=1 -v ./internal/archive -run 'Stage|ArchiveCoordinator|ArchiveNativeHandles|ExportThenVerify|ExportEntryBoundDuringListing|N4dSnapshotMovedDuringExport|N4fStagingRefused|ExportRefusesInconsistentStores|N4eReceiptCountBeyondHead|ContentAddressedNames|EncodedStreamLimit|EncodedLimitIncludesPAX|BytesAndSourceError|TerminalReadError|PartialDelivery'`
  — PASS; `tcp02-j3-02-focused-archive.log`.
- `go test -count=1 -v ./internal/snapshot ./internal/journal ./internal/archive -run 'Stage|Staging|NativeDirectoryObservationLifetime|JournalReadHandles|FileClosureErrors|ArchiveCoordinator|ArchiveNativeHandles|EmptyTicket|IntentDoesNotConsumeStateScanBudget'`
  — PASS; `tcp02-j3-02-focused-final.log` (before the later linkage amendment).
- `go test -count=1 -v ./internal/snapshot ./internal/journal ./internal/archive -run 'CompletedStage|StageReadStableBindingsAndPurity|JournalStageReadBindings|StageGenesisAndNext|StageObservation'`
  — PASS after linkage repair; `tcp02-j3-02-focused-linkage-repair.log`.
- `go test -count=1 -overlay /private/tmp/corvint-tasks-build-20260906/tcp02-j3-02-empty-d-red-overlay.json ./internal/journal -run EmptyTicketRequestLookup`
  — expected FAIL with original predicate; `tcp02-j3-02-empty-d-causal-red.log`.
- `go test -count=1 -v ./internal/journal -run 'EmptyTicketRequestLookup|EmptyTicketStrictReadBoundary'`
  — PASS without overlay; `tcp02-j3-02-empty-d-final-green.log`.
- Subsequent focused `go test -count=1 ./internal/journal -run 'JournalStageGenesisAndNext|GenesisProofCounterexamples|ChainInventoryAndPending|JournalStageReadBindings|JournalCompletedStageLabels'`
  — PASS after preserving legacy remnant reporting.
- Subsequent focused `go test -count=1 ./internal/journal ./internal/archive -run CompletedStageRequestBlob`
  and `go test -count=1 ./internal/archive -run StageExcludedFromFileCapBeforeBodies` — PASS.
- Final bound-order/linkage run:
  `go test -count=1 -v ./internal/snapshot ./internal/journal ./internal/archive -run 'StageObservation|CompletedStage|StageReadStableBindingsAndPurity|JournalStageReadBindings|StageExcludedFromFileCapBeforeBodies'`
  — PASS; `tcp02-j3-02-focused-final-binding.log`. The pure observer rejects excessive
  lengths before hashing supplied bytes; native acquisition already applies its read bounds.
- Final headless branch:
  `go test -count=1 ./internal/journal -run 'StageLinkedGenesisBeforeIntentProjection|GenesisProofCounterexamples|JournalStageGenesisAndNext|ChainInventoryAndPending'`
  — PASS; expanded temp/active/missing-blob subcases also passed the exact new test alone.
- Selected receipt-only genesis correction:
  `go test -count=1 -overlay /private/tmp/corvint-tasks-build-20260906/tcp02-j3-02-genesis-red-overlay.json ./internal/journal -run 'StageReceiptOnlyGenesisValidation/(receipt-only|partial)$'`
  — expected FAIL for receipt-only with the physical-queue requirement restored;
  `tcp02-j3-02-receipt-only-genesis-red.log`.
  `go test -count=1 -v ./internal/journal -run 'StageReceiptOnlyGenesisValidation|StageLinkedGenesisBeforeIntentProjection|GenesisProofCounterexamples|JournalStageGenesisAndNext|ChainInventoryAndPending'`
  — final PASS; `tcp02-j3-02-receipt-only-genesis-final.log`.
- `go test ./internal/transaction ./internal/cli -run '^$'` — compile-only PASS, no tests run.
- Owned-file gofmt and `git diff --check` — PASS. No `make verify`, project-wide tests or vet gate.

SPEC §5.6 lists the exact named tests. Finite coverage includes all absent/full slot subsets
(up to 2^11) across five shapes, each slot's empty/partial/full/digest/cap boundaries; strict
codec errors; no-active and active N−1/Nsame/Ndifferent/N+1 temp parity; empty raw evidence;
foreign/stale/request/completed binding; orphan retention; exact scan D and file F boundaries
before excess inspection/body opens; byte-preserving root/parent replacement; same-size and
restored-mtime changes; exactly four callbacks on persistent movement; validation/read errors
with stable versus moved tuples; failed recapture; one-shot inner probes; pending/fork/genesis;
sticky unlink/close/stream-cap errors including a later smaller successful stream; handle closure;
nonblocking FIFO/no-follow replacement; and bytes/mode/mtime/lock purity on success/refusal.

Additional named witnesses after the main enumeration:
`TestTMV0002_AS10_CompletedStageOperationAndTimestamp`,
`TestTMV0008_AS07_JournalCompletedStageLabels`,
`TestTMV0022_AS07_ArchiveCompletedStageLabels`,
`TestTMV0008_AS07_JournalCompletedStageRequestBlob`,
`TestTMV0022_AS07_ArchiveCompletedStageRequestBlob`,
`TestTMV0022_AS10_StageExcludedFromFileCapBeforeBodies`,
`TestTMV0009_AS11_StageLinkedGenesisBeforeIntentProjection`,
`TestTMV0009_AS11_StageReceiptOnlyGenesisValidation`.

## Holds and NOT_RUN

Parent canonical gate and fresh independent review: PENDING. No TCP-02 completion tick.
Native Linux execution, physical million-file/4-GiB fixtures, filesystem power-loss, production
RSS/performance and GP: NOT_RUN. No filesystem qualification follows from native read tests.
Archive historical semantics, full-plan acceptance, actor/issuer/admin authority, capacity
qualification, cleanup permission, existing-intent CAS, writer/restore/runtime and real-queue
cutover: NOT_OBSERVED or HELD under existing contracts. No new approval or publication follows.
